# Preprocessing requirements

No new configuration-specific requirement was proven. The generic constructor returned native error `-3`, but the public AEC helper succeeded in the same session. The framework source path therefore appears to perform implementation selection/setup beyond a generic type-only construction; Phase 17 does not identify the exact missing prerequisite for the generic path.

The running preprocessing library and prior static work establish the standard 16 kHz mono PCM16 input as a recognized configuration, but no patch or workaround was attempted.
