# Service PID timeline

```text
before Phase 17 creation: 4405
after Query/Create/Direct/Generic: 4405
```

No HIDL audio-service restart or tombstone was observed during the instrumented creation run. The earlier baseline `dumpsys` crash remains separately documented in Phase 16.
