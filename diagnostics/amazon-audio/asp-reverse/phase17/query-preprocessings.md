# Query preprocessings

Reflection attempted `AudioEffect.queryPreProcessings(int)`. This framework build has no such method:

```text
java.lang.NoSuchMethodException: android.media.audiofx.AudioEffect.queryPreProcessings [int]
```

No session preprocessing descriptor list could be obtained through this API surface.
