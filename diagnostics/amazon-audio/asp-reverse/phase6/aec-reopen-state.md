# AEC reopen state

The string `AECOn, need reopen the capture handle` is in the capture configuration path next to mode, input source, input device, and bypass state diagnostics. The exact old/new state variables and the complete close/reopen caller chain remain **UNKNOWN**. No claim is made that the transition is caused by source 7, mode 3, effect attachment, routing, or `SetForceAECRec`.
