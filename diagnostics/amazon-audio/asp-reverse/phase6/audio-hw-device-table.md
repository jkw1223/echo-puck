# `audio_hw_device` table

`AudioALSAHardware::setMode` and `AudioALSAHardware::setParameters` are confirmed C++ methods, but the device function-pointer table assignment and `open_input_stream` table entry were not recovered. The table mapping remains **UNKNOWN**.
