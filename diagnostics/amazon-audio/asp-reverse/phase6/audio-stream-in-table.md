# `audio_stream_in` table

The stripped ARM binary's stream table and the exact implementations of `set_parameters`, `add_audio_effect`, `remove_audio_effect`, and `read` were not recovered with confidence. Direct internal symbols and callers are preserved in the Phase 4/5 disassembly slices. The Android AudioFlinger -> HAL callback hop remains **UNKNOWN**.
