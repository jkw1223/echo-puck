# Deferred experiment

No experiment is justified yet. The smallest future test would first establish a read-only effect registration snapshot and prove that a framework AEC descriptor can be instantiated. Only then should a reversible session-scoped `AcousticEchoCanceler.create(sessionId)` test be considered. No configuration injection, device modification, `SetAudioCommand(0xa4, ...)`, vendor patch, or ASP call was performed.
