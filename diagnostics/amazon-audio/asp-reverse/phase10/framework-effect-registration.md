# Framework effect registration

The HAL recognizes the standard AEC type UUID internally, but no retained Android 11 effect-factory XML/CONF or runtime factory dump is available. Therefore the framework-side `EffectsFactory` edge remains UNKNOWN. `AcousticEchoCanceler.isAvailable()==false` cannot be attributed to one specific missing file or library from current evidence.
