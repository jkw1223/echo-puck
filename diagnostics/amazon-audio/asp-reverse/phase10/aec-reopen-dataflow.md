# AEC reopen data flow

The exact `AECOn, need reopen the capture handle` diagnostic remains in the capture configuration path. The recovered `+0x48` callback (`AudioALSAStreamIn::set`) does not contain that message or a direct preprocess call. The old/new AEC comparison and subsequent close/reopen sequence remain UNKNOWN. No runtime state was collected.
