# Deferred runtime test

No runtime test is justified. Before activation, install/use an `adb` toolchain only for read-only collection, capture audioserver maps/dumpsys and effect configuration, then statically connect the preprocess setup callers to framework effect creation. Do not register, inject, or enable an effect until that chain is proven.
