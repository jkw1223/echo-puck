# Runtime effect inventory

No `AudioEffect.queryEffects()` descriptor list was collected in Phase 11. The known observation remains `AcousticEchoCanceler.isAvailable() == false`; type/implementation/name/implementor descriptors on the running Show are UNKNOWN.
