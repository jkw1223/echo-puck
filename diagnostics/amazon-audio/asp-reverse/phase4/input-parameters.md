# Input-stream parameters

No exact input-stream vendor parameter was confirmed. The HAL logs and symbols show `input_source` being consumed in capture configuration and a capture-handle AEC transition, but the available exports do not expose a named `in_set_parameters` implementation. No speculative parameter was sent to the device.
